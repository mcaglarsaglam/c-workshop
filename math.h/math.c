#include <math.h>
#include <stdio.h>

main()
{

    // ceil floor sqrt pow abs

    printf("%f\n", ceil(4.2));
    printf("%f\n", floor(4.9));
    printf("%f\n", sqrt(45));
    printf("%f\n", pow(3, 4));
    printf("%d\n", abs(15));

    return 0;
}